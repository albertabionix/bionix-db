# Environment Variables
.bionixdb/
__pycache__
*.egg-info/
.pytest_cache/

# Google Drive Authentication
credentials.json
quickstart.py
token.json

# Bundled OAuth client shipped with the package itself — intentionally tracked.
!src/bionix_db/credentials.json